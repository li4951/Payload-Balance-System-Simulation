1.维护一套PLNODE代码。
2.用户的快速查找方法。
3.PLNODE可方便的换成多个。命令行参数就能解决。
4.测试的时候可以考虑shell形式的。
5.能够设置PLNODE的个数。
6.总控防瘫痪。 fork execl


QUESTION：
主控节点切换之后采用相同的端口号还是不同的端口号？如果是不同的端口号，怎么保证测试进程段能按此端口访问服务。
答：可以考虑让副控节点只是一个监控进程，监督主控节点是否正常运行。如果主控over掉，可以通过fork子进程，在exec主进程代码。

